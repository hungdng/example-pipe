import { FormatDataArrayPipe } from './format-data-array.pipe';

describe('FormatDataArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDataArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
